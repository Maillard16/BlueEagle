<p id="notice"><%= notice %></p>

<h1>Notifications</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Message</th>
      <th>Etat</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @notifications.each do |notification| %>
      <tr>
        <td><%= notification.contenu %></td>
        <td><%= notification.etat %></td>
				<td><%= link_to 'Editer', edit_notification_path(notification) %></td>
        <td><%= link_to 'Effacer', notification, method: :delete, data: { confirm: 'Etss-vous sûr?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<!--<%= link_to 'New Notification', new_notification_path %>-->